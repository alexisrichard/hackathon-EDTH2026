# Core data
duckdb
pyarrow
pandas

# Geo
geopandas
shapely
pyogrio
pyproj

# HTTP + downloads
requests
httpx
tqdm

# AWS — let pip resolve compatible boto3 / aiobotocore / s3fs
boto3
s3fs

# AIS-specific
pyais

# OSM extracts
overpy

# Camera feeds (capture_camera_clip.py) — also needs ffmpeg on PATH (system binary)
yt-dlp

# Sentinel / Copernicus
sentinelhub

# Notebooks
jupyterlab
matplotlib
folium
