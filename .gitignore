# Claude Code local settings + runtime state (per-user, not for the repo)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.local

# Node
node_modules/
dist/
build/
.next/

# Data — big binary stuff lives on S3, never in git
*.parquet
*.tif
*.tiff
*.zip
*.rar
*.tar.gz
*.nc
*.h5

# Big-data subdirs (local mirrors of S3 if you sync) — kept out of git
data/ais/
data/sar/
data/optical/
data/cache/
data/tmp/
data/reference/raw/
raw/

# Auto-generated per-vessel lookup outputs — re-runnable, mirrored to S3
data/reference/equasis/
data/reference/gfw_events/
data/reference/gfw_vessels/

# Small, version-controlled layers (geo/criticality, reference, incidents)
# are intentionally tracked under data/geo/ and data/reference/.

# Large derived layers (>10 MB) — fetched from S3 via scripts/common/sync_from_s3.py
data/geo/emodnet_cables_combined.geojson
data/geo/marine_regions_eez_baltic.geojson
data/geo/ne_ocean_10m.geojson
data/geo/bathymetry_*.nc
data/geo/bathymetry_*.tif
data/geo/osm_buoys.geojson

# Generated demo outputs — re-runnable locally
demo_map_*.html
sar_*.png
comparison_before_after.png
detections_yi_peng3.geojson
*.ipynb

# Notebook checkpoints
.ipynb_checkpoints/

# Logs
*.log

# Computer-vision / SAR ship-detection pipeline (HRSID) — artifacts live on S3, not git.
# Prepared YOLO dataset (~5.6k PNG tiles + labels), training runs, trained weights:
data/hrsid_yolo/
scoring/runs/
scoring/weights/
# Generated dataset YAML holds a machine-specific absolute path:
scoring/hrsid.yaml
# Detector outputs:
detections_*.geojson
# YOLO pretrained checkpoints auto-downloaded by ultralytics into the repo root:
yolov8*.pt
yolo11*.pt
yolo26*.pt
